---
title: "AI Literacy Handbook"
author: "VU Education Lab"
date: "2025-09-14"
lang: en
toc: true          # Table of contents
toc-depth: 3       # Levels in TOC
number-sections: true


format:
  html:
    theme: Cosmo            # Bootstrap theme (cosmo, flatly, etc.)
    css: styles.css       # Custom CSS
    toc: true             # Table of contents
    toc-location: left
    code-fold: true       # Collapsible code blocks
    code-tools: true      # Copy button for code
    smooth-scroll: true
    number-sections: true
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

Agenda 8 May 2025 TT2.2 Hub Digital Society and Global Citizenship

Present: **Silvester Draaijer (VUA), Ragna Kemp Haraldsdóttir (UIce), Jorim Tielbeek,** Hilde Remmers (VUA), Maiza Campos (VUA), **Thao Duong (VUA),** Soren Daehn (CBS), Marketa Semberova (UPOL), **Zohra Hassan-Pieper (UDE – also WP2 project coordinator).**

$$
s f a s f \frac{d y}{d x} \iiint_{}^{}
$$

# The Annual in Paris
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).
